server {
    listen {{ nginx_port }};
    server_name localhost;

    location /files {
        alias /var/www/html/public_files/; 
        autoindex on;            
        autoindex_exact_size off;
        autoindex_localtime on;
    }
}
